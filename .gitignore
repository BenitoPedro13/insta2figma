node_modules
dist
.turbo
*.log
.DS_Store
.env
.env.*
!.env.example
apps/api/.env
apps/worker/.env
.pnpm-store
# Legado Figma (gerado ao lado do fonte)
code.js
scripts/.proxy-session
scripts/accounts.json
scripts/sessions.json

# Backup local do bucket Tigris (migração Railway) — não commitar imagens
bucket-backup/
